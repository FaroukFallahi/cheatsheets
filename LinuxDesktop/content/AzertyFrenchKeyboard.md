# Azerty French Keyboard
I began learning French through [Duolingo](https://www.duolingo.com) and
naturally, i thought i should know how to type french on my computer. So i
searched and found out that French uses a different type of keyboard called
`AZERTY` instead of our good-old `QWERTY`. The learning curve isn't steep and
here i'm going to tell you a few tricks to be able to type all those weird
diacritics that you find on the vowels. So let's begin.

## Key positions
I use Linux (obviously) and funny thing is there is a figure in gnome desktop
indicating the key layout. You can see that below but you can also see it by
going to the settings and click the `eye icon` right beside French in the
`Region & Language` section (of course you need to add the layout first).

![Azerty keyboard layout](./images/azerty-layout.png)

So now you have a very good high-level understanding of the key locations, but
still, some characters are not there (hell i even can't find the one which
combines `o` and `e` yet!).

## Special characters

### Keys found directly on the azerty keyboard
- é: The number `2` right above `w`
- è: The number `7` right above `u`
- ç: The number `9` right above `o`
- à: The number `0` right above `p`
- ù: The single quote (`'`) on the right side of semicolon (;)

### Keys that use `right alt` or `Alt Gr`
- æ: `right alt` + a
- Æ: `right alt` + A
- “: `right alt` + v
- ”: `right alt` + b
- ‘: `right alt` + V
- ’: `right alt` + B

## Special diacritics 
Note that i have wrote the key combinations as if i was writing on a qwerty
keyboard so that you'll be able to find them easily. So for instance
`[` would mean the second key from right beside the `enter` key.

Also note that to add these special diacritics, you must type them first, then
type the character that you want to add the diacritics to.

### Use the caret dead key

- â: ( `[` ) + a
- ẑ: ( `[` ) + z
- ê: ( `[` ) + e
- ŷ: ( `[` ) + y
- û: ( `[` ) + u
- î: ( `[` ) + i
- ô: ( `[` ) + o
- ŝ: ( `[` ) + s
- ĝ: ( `[` ) + g
- ĥ: ( `[` ) + h
- ĵ: ( `[` ) + j
- ŵ: ( `[` ) + w
- ĉ: ( `[` ) + c

### Use the reversed-caret dead key

- ǎ: ( `right alt` + `'` ) + a
- ž: ( `right alt` + `'` ) + z
- ě: ( `right alt` + `'` ) + e
- ř: ( `right alt` + `'` ) + r
- š: ( `right alt` + `'` ) + s
- ď: ( `right alt` + `'` ) + d
- ǧ: ( `right alt` + `'` ) + g
- ȟ: ( `right alt` + `'` ) + h
- ǰ: ( `right alt` + `'` ) + j
- ǩ: ( `right alt` + `'` ) + k
- ľ: ( `right alt` + `'` ) + l
- č: ( `right alt` + `'` ) + c
- ǚ: ( `right alt` + `'` ) + v
- ň: ( `right alt` + `'` ) + n

### Use the straight tilde dead key

- ā: ( `right alt` + `}` ) + a
- ē: ( `right alt` + `}` ) + e
- ȳ: ( `right alt` + `}` ) + y
- ū: ( `right alt` + `}` ) + u
- ī: ( `right alt` + `}` ) + i
- ō: ( `right alt` + `}` ) + o
- ḡ: ( `right alt` + `}` ) + g
- ǖ: ( `right alt` + `}` ) + v

### Use the back-tick dead key

- à: ( `right alt` + `\` ) + a
- ỳ: ( `right alt` + `\` ) + y
- ù: ( `right alt` + `\` ) + u
- ì: ( `right alt` + `\` ) + i
- ò: ( `right alt` + `\` ) + o
- ẁ: ( `right alt` + `\` ) + w
- ǜ: ( `right alt` + `\` ) + v
- ǹ: ( `right alt` + `\` ) + n

### Use the forward-tick dead key

- á: ( `right alt` + `m` ) + a
- ź: ( `right alt` + `m` ) + z
- é: ( `right alt` + `m` ) + e
- ŕ: ( `right alt` + `m` ) + r
- ý: ( `right alt` + `m` ) + y
- ú: ( `right alt` + `m` ) + u
- í: ( `right alt` + `m` ) + i
- ó: ( `right alt` + `m` ) + o
- ṕ: ( `right alt` + `m` ) + p
- ś: ( `right alt` + `m` ) + s
- ǵ: ( `right alt` + `m` ) + g
- ḱ: ( `right alt` + `m` ) + k
- ĺ: ( `right alt` + `m` ) + l
- ḿ: ( `right alt` + `m` ) + m
- ẃ: ( `right alt` + `m` ) + w
- ć: ( `right alt` + `m` ) + c
- ǘ: ( `right alt` + `m` ) + v
- ń: ( `right alt` + `m` ) + n

### Use the double-forward-tick dead key

- ű: ( `right alt` + `M` ) + u
- ő: ( `right alt` + `M` ) + o

### Use the top-dot dead key

- ȧ: ( `right alt` + `?` ) + a
- ż: ( `right alt` + `?` ) + z
- ė: ( `right alt` + `?` ) + e
- ṙ: ( `right alt` + `?` ) + r
- ṫ: ( `right alt` + `?` ) + t
- ẏ: ( `right alt` + `?` ) + y
- ı: ( `right alt` + `?` ) + i
- ȯ: ( `right alt` + `?` ) + o
- ṗ: ( `right alt` + `?` ) + p
- ṡ: ( `right alt` + `?` ) + s
- ḋ: ( `right alt` + `?` ) + d
- ḟ: ( `right alt` + `?` ) + f
- ġ: ( `right alt` + `?` ) + g
- ḣ: ( `right alt` + `?` ) + h
- ȷ: ( `right alt` + `?` ) + j
- ŀ: ( `right alt` + `?` ) + l
- ṁ: ( `right alt` + `?` ) + m
- ẇ: ( `right alt` + `?` ) + w
- ẋ: ( `right alt` + `?` ) + x
- ċ: ( `right alt` + `?` ) + c
- ḃ: ( `right alt` + `?` ) + b
- ṅ: ( `right alt` + `?` ) + n

### Use the bottom-dot dead key

- ạ: ( `right alt` + `/` ) + 
- ẓ: ( `right alt` + `/` ) + 
- ẹ: ( `right alt` + `/` ) + 
- ṛ: ( `right alt` + `/` ) + 
- ṭ: ( `right alt` + `/` ) + 
- ỵ: ( `right alt` + `/` ) + 
- ụ: ( `right alt` + `/` ) + 
- ị: ( `right alt` + `/` ) + 
- ọ: ( `right alt` + `/` ) + 
- ṣ: ( `right alt` + `/` ) + 
- ḍ: ( `right alt` + `/` ) + 
- ḥ: ( `right alt` + `/` ) + 
- ḳ: ( `right alt` + `/` ) + 
- ḷ: ( `right alt` + `/` ) + 
- ṃ: ( `right alt` + `/` ) + 
- ẉ: ( `right alt` + `/` ) + 
- ṿ: ( `right alt` + `/` ) + 
- ḅ: ( `right alt` + `/` ) + 
- ṇ: ( `right alt` + `/` ) + 

### Use the double-top-dot dead key

- ä: ( `{` ) + a
- ë: ( `{` ) + e
- ẗ: ( `{` ) + t
- ÿ: ( `{` ) + y
- ü: ( `{` ) + u
- ï: ( `{` ) + i
- ö: ( `{` ) + o
- ḧ: ( `{` ) + h
- ẅ: ( `{` ) + w
- ẍ: ( `{` ) + x

### Use the curvy-back-tick dead key

- ả: ( `right alt` + `j` ) + a
- ȥ: ( `right alt` + `j` ) + z
- ẻ: ( `right alt` + `j` ) + e
- ɼ: ( `right alt` + `j` ) + r
- ƭ: ( `right alt` + `j` ) + t
- ỷ: ( `right alt` + `j` ) + y
- ủ: ( `right alt` + `j` ) + u
- ỉ: ( `right alt` + `j` ) + i
- ỏ: ( `right alt` + `j` ) + o
- ƥ: ( `right alt` + `j` ) + p
- ʠ: ( `right alt` + `j` ) + q
- ʂ: ( `right alt` + `j` ) + s
- ɗ: ( `right alt` + `j` ) + d
- ƒ: ( `right alt` + `j` ) + f
- ɠ: ( `right alt` + `j` ) + g
- ɦ: ( `right alt` + `j` ) + h
- ƙ: ( `right alt` + `j` ) + k
- ɱ: ( `right alt` + `j` ) + m
- ⱳ: ( `right alt` + `j` ) + w
- ƈ: ( `right alt` + `j` ) + c
- ʋ: ( `right alt` + `j` ) + v
- ɓ: ( `right alt` + `j` ) + b
- ɲ: ( `right alt` + `j` ) + n

### Use the curvy-forward-tick dead key

- ư: ( `right alt` + `J` ) + u
- ơ: ( `right alt` + `J` ) + o

### Use the reversed-curvy-back-tick dead key

- ą: ( `right alt` + `+` ) + a
- ę: ( `right alt` + `+` ) + e
- ų: ( `right alt` + `+` ) + u
- į: ( `right alt` + `+` ) + i

### Use the circle dead key

- å: ( `right alt` + `{` ) + a
- ẙ: ( `right alt` + `{` ) + y
- ů: ( `right alt` + `{` ) + u
- ẘ: ( `right alt` + `{` ) + w

### Use the bottom-half-semi-circle dead key

- ă: ( `right alt` + `|` ) + a
- ĕ: ( `right alt` + `|` ) + e
- ŭ: ( `right alt` + `|` ) + u
- ĭ: ( `right alt` + `|` ) + i
- ŏ: ( `right alt` + `|` ) + o
- ğ: ( `right alt` + `|` ) + g
